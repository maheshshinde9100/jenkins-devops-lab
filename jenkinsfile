pipeline {
    agent any

    stages {

        stage('Install Dependencies') {
            steps {
                bat 'cd backend && npm install'
            }
        }
        
        stage('Check Syntax') {
            steps {
                bat 'cd backend && node --check server.js'
            }
        }

    }
}